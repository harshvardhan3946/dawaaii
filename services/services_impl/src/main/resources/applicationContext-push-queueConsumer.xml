<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd"

       default-lazy-init="false">

    <!--
    <bean id="pushNotificationContainer" class="org.springframework.jms.listener.DefaultMessageListenerContainer">
        <property name="connectionFactory" ref="jmsFactory"/>
        <property name="destination" ref="pushQueue"/>
        <property name="messageListener">
            <bean class="org.springframework.jms.listener.adapter.MessageListenerAdapter">
                <property name="delegate" ref="pushNotificationProcessor"/>
                <property name="defaultListenerMethod" value="sendNotification"/>
                <property name="messageConverter" ref="simpleMessageConverter"/>
            </bean>
        </property>
        <property name="concurrentConsumers" value="${queue.crash.push.concurrentConsumers}" />
        <property name="maxConcurrentConsumers" value="${queue.crash.push.maxConcurrentConsumers}" />
        <property name="sessionTransacted" value="false"/>
        <property name="autoStartup" value="true"/>
    </bean>
    -->
</beans>